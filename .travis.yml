language: cpp

compiler:
 - clang
 - gcc-4.8
 
before_script:
 - g++ --version
 - clang++ --version

script:
 - cd ./test
 - make
 - make STDFLAG=-std=c++98
 - make CC=clang CXX=clang++ STDFLAG=-std=c++98
 - make CC=clang CXX=clang++ STDFLAG=-std=c++1y
 

install:
 - sudo add-apt-repository --yes ppa:ubuntu-toolchain-r/test
 - wget -O - http://llvm.org/apt/llvm-snapshot.gpg.key | sudo apt-key add -
 - sudo add-apt-repository --yes 'deb http://llvm.org/apt/precise/ llvm-toolchain-precise main'
 - sudo apt-get -qq update
 - sudo apt-get -qq install g++-4.8
 - sudo apt-get -qq install clang-3.5
 

