before_script:
  - cat /proc/cpuinfo || true
  - cat /proc/meminfo || true
  - nproc
  - gcc --version || true
  - clang --version || true
script:
  - make -C test -j$((`nproc`+1))
  - make -C test test
