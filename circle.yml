machine:
  python:
    version: 3.5.0

dependencies:
  pre:
    - pip install requests

test:
  override:
    - cd tools/fused; make;
    - cd tools/wandbox; make test;
    - cd tools/twilio; make dryrun CALL_NUMBER=+819012345678
